Module Program

    Sub Main()
        App.Start()
    End Sub
    Public Sub Action(znak As Object, A As Double, B As Double)
        App.show(znak, A, B)
        App.enterKey()
    End Sub
End Module
